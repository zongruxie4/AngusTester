# DeepSeek API configuration
api.key=your_api_key_here

# Optional overrides
api.endpoint=https://api.deepseek.com/chat/completions
prompt.template=Translate to {targetLanguage} with technical accuracy

# Retry configuration
retry.max=3
retry.initialDelayMs=1000
retry.backoffFactor=2.0

# Timeout settings
timeout.seconds=15
