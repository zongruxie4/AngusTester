# DeepSeek API configuration
api.key=your_api_key_here
api.secret=your_api_secret_here
#api.regionId=cn-hangzhou

# Optional overrides
api.endpoint=mt.cn-hangzhou.aliyuncs.com

# Retry configuration
retry.max=3
retry.initialDelayMs=1000
retry.backoffFactor=2.0

# Timeout settings
timeout.seconds=15
