--
-- Copyright (c) 2021   XCan Company
--
--        http://www.xcan.cloud
--
-- Licensed under the XCBL(XCan Business License) Version 1.0.
-- Detail XCBL license at:
--
-- http://www.xcan.cloud/licenses/XCBL-1.0
--

-- 1. Create and grant User（Execute via root account）
-- CREATE USER 'angustester'@'%' IDENTIFIED BY 'BEGINsi7Hvuc';
-- grant all privileges on xcan_angustester.* to angustester@'%' identified by 'BEGINsi7Hvuc';
-- flush privileges;

-- 2. Create database（Execute via root account）
-- CREATE DATABASE `xcan_angustester` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_bin ;

-- 3. Create table

-- 4. Drop all（Execute via root account）
-- DROP DATABASE IF EXISTS xcan_angustester;
-- DROP USER IF EXISTS angustester;

