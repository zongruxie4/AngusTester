// 图标类型
export enum ChartType {
  PIE = 'PIE',
  LINE = 'LINE'
}

// GroupBy枚举
export enum GroupBy {
  STATUS = 'STATUS',
  DATE = 'DATE'
}

// 聚合函数枚举
export enum AggregateFunction {
  COUNT = 'COUNT',
  SUM = 'SUM',
  AVG = 'AVG',
  MAX = 'MAX',
  MIN = 'MIN'
}

// 日期范围类型枚举
export enum DateRangeType {
  DAY = 'DAY',
  WEEK = 'WEEK',
  MONTH = 'MONTH',
  YEAR = 'YEAR',
  HOUR = 'HOUR'
}
